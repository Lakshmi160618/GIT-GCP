global_variable = 42

def access_global():
    print("Global variable:", global_variable)

# Call the function
access_global()

'''
In this example, there's a global variable global_variable, and the access_global function can access and use this global variable within the function's scope.
'''