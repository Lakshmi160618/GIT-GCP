add = lambda x, y: x + y

# Use the lambda function
result = add(5, 3)
print("Sum:", result)
list = [2,3,4,3,4,5,6]
# with out using set function you need to remove all the dupes from the list
print(list(set(list)))
# you need to sort this list without using sort method
"""
In this example, a lambda function add is defined, which takes two parameters and returns their sum. Lambda functions are typically used for small, simple operations.
"""